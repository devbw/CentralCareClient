import React from 'react';

const spinner = () => {
  return (
    <div className="container-spinner">
      <i className="fas fa-spinner"></i>
      <p>Patience, nous chargeons le contenu !</p>
    </div>
  );
};

export default spinner;